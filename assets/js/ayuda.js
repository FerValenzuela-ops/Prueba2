/* Esta funcion identifica el span con los parametros indicados para que se muestre el mensaje personalizado en
en los textbox de registro */
function setVisibility(id, visibility) {
    document.getElementById(id).style.display = visibility;
}